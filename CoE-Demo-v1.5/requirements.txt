rich>=13.0.0
colorama>=0.4.6
platformdirs>=3.0.0
numpy>=1.24.0
huggingface_hub>=0.19.0

# Embedding — sentence-transformers + BAAI/bge-m3 (replaces fastembed)
sentence-transformers>=2.6.0
torch>=2.0.0  # optional; only required for GPU-accelerated embedding

tiktoken>=0.5.0

# ONNX Supervisor — install exactly ONE for your hardware:
#   pip install onnxruntime-directml   # AMD APU / Windows Copilot+ PCs (DirectML)
#   pip install onnxruntime-gpu        # NVIDIA RTX (CUDA)
#   pip install onnxruntime            # Mac / CPU-only fallback
onnxruntime-genai>=0.3.0  # OGA wrapper — required for the Nanbeige supervisor model
